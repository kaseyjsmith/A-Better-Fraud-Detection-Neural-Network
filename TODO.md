- [x] Make lightning save the best model (checkpointing, early stopping)
- [x] Host the model at an endpoint (API service, load model, model inference)
  - try out fastapi
  - [x] Create /predict endpoint
  - [x] Create service layer

- [ ] Change the data loaders to include a validation set. Right now just using train/test
- [ ] ~Create a requirements.txt file~ Makefile et. al

